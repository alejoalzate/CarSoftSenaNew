<tr id=<%= "vehicle_#{vehicle.id}" %>>
    <td><%= link_to vehicle.id, edit_vehicle_path(vehicle), :remote => true %></td>
    <td><%= vehicle.plate %></td>
    <td><%= vehicle.color %></td>
    <td><%= vehicle.brand %></td>
    <td><%= vehicle.model %></td>
    <td><%= vehicle.type_vehicle.name %></td>
    <td><%= vehicle.user.name %></td>
    <td>
        <%= link_to  image_tag('mostrar.png'), vehicle,     :remote => true, :class => 'btn btn-mini' %>
        <%= link_to image_tag('editar1.png', :title => "Editar Equipos"), edit_vehicle_path(vehicle),  :remote => true,
          :class => 'btn btn-mini'%>
        <%= link_to image_tag('eliminar.png', :title => "Eliminar Equipos"), vehicle, :data => { :confirm => 'Esta Seguro?' },
          :method => :delete, :remote => true, :class => 'btn btn-mini btn-danger' %>
    </td>
</tr>
