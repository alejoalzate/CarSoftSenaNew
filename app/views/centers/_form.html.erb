<%= simple_form_for([@area_center, @center], :html => { :class => 'form-horizontal' }, :remote => true) do |f| %>

    <%= f.error_notification %>
    <%= display_base_errors @center %>
    
    <p><%= f.input :name, :label => "Nombre",  :required => true  %></p>

    <p><%= f.input :code, :label => "Codigo",  :required => true  %></p>
   
    <%= f.label :area_center_id, :label => "Area Centro",  :required => true %>
           <%= f.collection_select :area_center_id , AreaCenter.all, :id, :name, :prompt => "Selecione Area Centro"   %>
    <div class="form-actions">
	    <%= f.submit "guardar", :class => 'btn btn-primary' %>
	    <a href="#" class="btn" onclick="$.lazybox.close()">Cerrar</a>
  	</div>
<% end %>
