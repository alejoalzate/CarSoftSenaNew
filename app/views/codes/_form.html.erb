<%= simple_form_for @code, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :code %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                codes_path, :class => 'btn' %>
  </div>
<% end %>
